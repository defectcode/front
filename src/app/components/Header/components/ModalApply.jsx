

function ModalApply({ onClick }) {
    return (
      <button
        onClick={onClick}
        className="bg-black text-white rounded-lg px-4 py-2 hover:bg-gray-800"
      >
        Apply Now
      </button>
    );
  }